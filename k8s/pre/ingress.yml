apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    alb.ingress.kubernetes.io/backend-protocol: grpc
    alb.ingress.kubernetes.io/healthcheck-port: '6334'
    alb.ingress.kubernetes.io/healthcheck-protocol: TCP
    alb.ingress.kubernetes.io/listen-ports: '[{"HTTPS": 443}]'
  labels:
    ingress-controller: alb
  name: qdrant-ingress
  namespace: pre
spec:
  ingressClassName: alb
  rules:
    - host: qdrant-pre.atominnolab.com
      http:
        paths:
          - backend:
              service:
                name: qdrant-headless
                port:
                  number: 6334
            path: /
            pathType: Prefix
    - host: qdrant.wispaperpre.inner
      http:
        paths:
          - backend:
              service:
                name: qdrant-headless
                port:
                  number: 6334
            path: /
            pathType: Prefix
  tls:
    - hosts:
        - qdrant-pre.atominnolab.com
      secretName: ssl-atominnotab.com